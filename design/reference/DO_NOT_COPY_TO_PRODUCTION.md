# Static reference warning

The `.dc.html`, `support.js`, `ios-frame.jsx` and `_ds` files reproduce the design review canvas. They are reference artefacts, not production dependencies.

Reimplement the product as maintainable React components using the visual hierarchy and grammar. Do not ship these helper files in the web application bundle.
