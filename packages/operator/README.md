# `packages/operator`

MCP input/output schemas, operator mutation validation, snapshot concurrency, stable-key deduplication and skill-adjacent contracts. It cannot expose external execution capabilities to the scheduled operator.
