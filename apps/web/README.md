# `apps/web`

Next.js App Router application containing the responsive dashboard, authenticated browser APIs, Google OAuth callbacks and isolated remote MCP endpoint. Do not put long-running queue execution in Vercel request handlers.
